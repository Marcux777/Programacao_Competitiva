#include <bits/stdc++.h>
using namespace std;

#define endl '\n'

vector<vector<int>> dp[2];

int f(int max, int jogada){
    if(dp[])
}


int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    int n, m; cin >> n;
    if(n == 1 || n == 4){
        
    }
}