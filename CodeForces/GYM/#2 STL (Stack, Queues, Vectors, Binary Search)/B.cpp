#include <bits/stdc++.h>

using namespace std;

#define int long long int

signed main(){
    int n, q; 
    cin >> n >> q;
    vector<int> v(n);
    for(int i = 0; i < n; ++i) {
        cin >> v[i];
    }

    while(q--){
        
    }
}