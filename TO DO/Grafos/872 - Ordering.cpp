/*A ordem é um conceito importante na matemática e na ciência da computação. 
Por exemplo, o Lema de Zorn afirma: um conjunto parcialmente ordenado no qual 
cada cadeia tem um limite superior contém um elemento maximal. 
A ordem também é importante para o raciocínio sobre a semântica do ponto 
fixo de programas.
Este problema não envolve nem o Lema de Zorn nem a semântica do ponto fixo,
mas envolve ordem.
Dada uma lista de restrições de variáveis da forma A < B, 
você deve escrever um programa que imprime todas as ordenações das variáveis 
que são consistentes com as restrições. Por exemplo, dadas as restrições A < B e A < C, 
existem duas ordenações das variáveis A, B e C que são consistentes com 
essas restrições: ABC e ACB.
Entrada
A entrada começa com um único inteiro positivo em uma linha, indicando o 
número de casos que seguem, cada um deles conforme descrito abaixo. 
Esta linha é seguida por uma linha em branco, e também há uma linha em 
branco entre duas entradas consecutivas.
A entrada consiste em duas linhas: uma lista de variáveis em uma linha 
seguida por uma lista de restrições da forma A < B na próxima linha. 
Variáveis e restrições são separadas por espaços simples.
Todas as variáveis são letras maiúsculas de um único caractere. 
Haverá pelo menos duas variáveis e no máximo 20 variáveis. Haverá pelo menos uma 
restrição e no máximo 50 restrições. Não haverá mais do que 300 ordenações 
consistentes com as restrições em uma especificação.
Saída
Para cada caso de teste, a saída deve seguir a descrição abaixo. 
As saídas de dois casos consecutivos serão separadas por uma linha em branco.
Todas as ordenações consistentes com as restrições devem ser impressas. 
As ordenações são impressas em ordem alfabética, uma por linha. Os caracteres 
em uma linha são separados por um espaço. Se não for possível nenhuma ordenação, 
a saída é uma única linha com a palavra 'NO'.*/

#include <bits/stdc++.h>

using namespace std;

int main(){
    int t; cin >> t;

    while(t--){
        
    }
}