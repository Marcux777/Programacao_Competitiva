#include <bits/stdc++.h>

using namespace std;

#define int long long
#define IOS                           \
    ios_base::sync_with_stdio(false); \
    cin.tie(0)

signed main() {
    IOS;
    string s; cin >> s;
    sort(s.begin(), s.end());
    cout << s << endl;
}
