#include <bits/stdc++.h>

using namespace std;

#define int long long
#define IOS                           \
    ios_base::sync_with_stdio(false); \
    cin.tie(0)

signed main() {
    IOS;
    string s;
    while(getline(cin, s)) {
        int n = s.size();
        for(int i = 0; i < n; i++){
            
        }

    }
}
